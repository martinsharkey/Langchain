# V2.0 PHASE 1: ARCHITECTURE KICKOFF

**Date:** 2026-08-25
**Duration:** Weeks 1-2
**Status:** STARTING NOW
**Goal:** Extract services from monolith, setup Docker infrastructure

---

## WEEK 1 TASKS

### Task 1.1: Create Shared Layer (Monday-Tuesday)

**Objective:** Build foundation that all services depend on

**Deliverables:**
- [x] shared_models.py (created)
  - Request/Response dataclasses
  - Service interfaces (Abstract)
  - Exceptions
  
- [ ] shared/utils/ (to create)
  - logging.py
  - config.py
  - decorators.py
  - helpers.py

- [ ] shared/integrations/ (to create)
  - database.py (PostgreSQL)
  - message_queue.py (RabbitMQ)
  - langchain_ai.py (OpenAI integration)
  - broker_adapter.py

**Tests Required:**
- Test model validation
- Test service interfaces
- Test exception hierarchy

---

### Task 1.2: Discovery Service Extraction (Wednesday)

**Objective:** Extract Phase 1 logic into independent microservice

**Source:** src/phase1_discovery.py
**Target:** services/discovery-service/

**Structure:**
```
services/discovery-service/
├── app/
│   ├── main.py              # FastAPI app
│   ├── models.py            # Request/response models
│   ├── routes.py            # /api/v1/discovery/* endpoints
│   └── dependencies.py      # Dependency injection
├── core/
│   ├── discovery_engine.py  # Phase 1 logic (from phase1_discovery.py)
│   ├── strategy_explorer.py # AI-powered exploration (new)
│   └── metrics.py           # Metric calculations
├── Dockerfile
├── requirements.txt
├── .env.example
└── README.md
```

**Endpoints to Implement:**
```
POST   /api/v1/discovery/start
GET    /api/v1/discovery/{job_id}/status
GET    /api/v1/discovery/{job_id}/results
GET    /api/v1/discovery/strategies
```

**Tests:**
- test_discovery_engine.py
- test_discovery_api.py
- test_discovery_integration.py

---

### Task 1.3: Optimization Service Extraction (Thursday)

**Objective:** Extract Phase 2 logic into independent microservice

**Source:** src/phase2_tuning.py
**Target:** services/optimization-service/

**Structure:**
```
services/optimization-service/
├── app/
│   ├── main.py
│   ├── models.py
│   ├── routes.py
│   └── dependencies.py
├── core/
│   ├── optimization_engine.py  # Phase 2 logic (from phase2_tuning.py)
│   ├── parameter_suggester.py  # AI parameter suggestions
│   └── trial_manager.py
├── Dockerfile
├── requirements.txt
└── README.md
```

**Endpoints:**
```
POST   /api/v1/optimization/start
GET    /api/v1/optimization/{job_id}/status
GET    /api/v1/optimization/{job_id}/results
GET    /api/v1/optimization/{job_id}/trials
```

---

### Task 1.4: Validation Service Extraction (Friday AM)

**Objective:** Extract Phase 3 logic into independent microservice

**Source:** src/phase3_validation.py
**Target:** services/validation-service/

**Endpoints:**
```
POST   /api/v1/validation/start
GET    /api/v1/validation/{job_id}/status
GET    /api/v1/validation/{job_id}/decision
```

---

### Task 1.5: Docker Infrastructure Setup (Friday PM)

**Objective:** Create docker-compose for local development

**Deliverables:**
```
docker-compose.yml
├── discovery-service (port 8001)
├── optimization-service (port 8002)
├── validation-service (port 8003)
├── deployment-service (port 8004)
├── execution-service (port 8005)
├── orchestrator-service (port 8006)
├── api-gateway (port 8000)
├── PostgreSQL (port 5432)
├── Redis (port 6379)
└── RabbitMQ (port 5672)
```

**Testing:**
```bash
docker-compose up -d
docker-compose ps  # All 9 services running
```

---

## WEEK 2 TASKS

### Task 2.1: Deployment Service Extraction (Monday)

**Source:** src/phase4_deployment.py
**Target:** services/deployment-service/

---

### Task 2.2: Orchestrator Service Creation (Tuesday-Wednesday)

**Objective:** Create new orchestrator service (doesn't exist in v1.0)

**New Service - services/orchestrator-service/**

```
Orchestrator responsibilities:
1. Receive pipeline start request
2. Call Discovery Service
3. Call Optimization Service (per strategy)
4. Call Validation Service
5. Call Deployment Service
6. Return final deployment status

Pattern: Temporal Workflow
```

**API Endpoints:**
```
POST   /api/v1/pipeline/start
GET    /api/v1/pipeline/{job_id}/status
GET    /api/v1/pipeline/{job_id}/logs
```

---

### Task 2.3: Execution Service Creation (Thursday)

**Objective:** New service (formerly ScalpEngine) for live trading

**New Service - services/execution-service/**

Responsibilities:
1. Load tuned_params.json
2. Determine current session
3. Select strategy
4. Generate entry/exit signals
5. Execute trades
6. Track P&L

**API Endpoints:**
```
GET    /api/v1/execution/status
POST   /api/v1/execution/pause
POST   /api/v1/execution/resume
GET    /api/v1/execution/trades
GET    /api/v1/execution/performance
```

---

### Task 2.4: Service Integration Testing (Friday)

**Objective:** Test service-to-service communication

**Tests:**
- discovery → optimization flow
- optimization → validation flow
- validation → deployment flow
- Full pipeline integration

---

## SUCCESS CRITERIA (Phase 1)

- [ ] All 6 services extracted and working independently
- [ ] Docker-compose brings up all 9 containers
- [ ] Each service has REST API endpoints
- [ ] Each service has unit tests (>90% coverage)
- [ ] Integration tests passing
- [ ] Shared layer complete and tested
- [ ] No v1.0 code in services (properly migrated)
- [ ] All services can communicate via message queue
- [ ] Logging/monitoring infrastructure working
- [ ] Documentation for each service complete

---

## DAILY STANDUP FORMAT

**Each day (EOD):**
- [x] What was completed?
- [ ] What's blocking?
- [ ] What's next?

---

## CONTINGENCY PLANS

**If behind schedule:**
- Defer execution service to Week 3
- Deploy first 5 services (without execution)
- Continue with Phase 2 (API Gateway)

**If issues found:**
- Pause, investigate, fix
- Adjust timeline accordingly

---

## RESOURCES REQUIRED

**Team:**
- 3 Backend Engineers
- 1 DevOps Engineer
- 1 QA Engineer

**Infrastructure:**
- Docker
- PostgreSQL
- Redis
- RabbitMQ
- GitHub Actions

---

## DELIVERABLES

**End of Week 2:**
- 6 working microservices
- Docker-compose setup
- 150+ passing tests
- Complete service documentation

