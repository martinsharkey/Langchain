# Core LangChain & AI
langchain>=0.3.0,<0.4.0
langchain-groq>=0.2.0,<0.3.0
langgraph>=0.2.0,<0.3.0
langchain-community>=0.3.0,<0.4.0

# Multi-Provider LLM Router (LiteLLM)
# Provides access to Groq, Google Gemini, Cerebras, Mistral, OpenRouter,
# SambaNova, Cohere, Cloudflare, NVIDIA, HuggingFace, GitHub Models,
# Together AI, DeepSeek, AI21, xAI/Grok, and 100+ other providers
# through a single unified interface with automatic fallback
litellm>=1.40.0
langchain-litellm>=0.7.0

# Environment & Config
python-dotenv>=1.0.0

# Data Processing
pandas>=2.0.0
numpy>=1.24.0

# Technical Analysis
ta>=0.10.0

# MetaTrader 5 (Windows native, requires Wine on macOS)
# Install manually via: pip install MetaTrader5
# MetaTrader5>=5.0.45

# Vector Store & RAG (for meta-strategy learning system)
# Stores market patterns as vector embeddings for similarity search
chromadb>=1.10.0

# CLI & Output
rich>=13.0.0

# HTTP (for web search tool)
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0
